import React from 'react'
import Layout from '../components/Layout'

/*
 * Index page, currently not used for anything. See Home.js.
 */
const Index = (props) => (
  <Layout {...props} sidebar={false}>
    <h2>Index</h2>
  </Layout>
)

export default Index
